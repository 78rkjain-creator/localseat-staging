import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // PWA and performance settings will be added in the PWA polish phase
  devIndicators: false,
};

export default nextConfig;
