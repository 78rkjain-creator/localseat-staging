# Database
DATABASE_URL="postgresql://postgres:password@localhost:5432/localseat_dev"

# NextAuth
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"
CRON_SECRET="generate-a-random-secret-here"

# Mapbox — public token is required for client-side maps.
# Server token is optional; used for server-side geocoding if set.
# NEXT_PUBLIC_MAPBOX_TOKEN="pk.your-public-token"
# MAPBOX_SERVER_TOKEN="sk.your-server-token"
